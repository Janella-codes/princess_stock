export type UserProfileToken = {
  userId: string; // Define userId as a string property
  userName: string;
  email: string;
  token: string;
};
  
  export type UserProfile = {
    userName: string;
    email: string;
  };
  